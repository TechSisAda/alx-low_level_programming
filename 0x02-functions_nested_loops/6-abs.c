#include "main.h"
/**
* _abs - returns absolute value of int
*@a: integer from which to get an abs value
* Return: absolute value of number a.
*/
int _abs(int a)
{
if (a >= 0)
return (a);
else
return (a * -1);
}

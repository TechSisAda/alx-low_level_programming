#include "main.h"
/**
 * set_string - sets value to a pointer char
 * @s: double pointer to a string
 * @to: pointer to string to set s
 * Return: description of returned value
 */
void set_string(char **s, char *to)
{
    *s = to;
}
